
version: 0.2
phases:
  install:
    commands:
      - echo Installing source NPM dependencies...
      - npm install -g @angular/cli
  build:
    commands:
      - echo "Entered build phase"
      - gradle build
artifacts:
  files:
    - build/libs/*
